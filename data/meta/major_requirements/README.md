# Major Requirements

Each `.csv` file defines the degree requirements for one major (CS, MATH, BA).

Columns: `GROUP`, `GROUP_TYPE`, `SUBJ`, `NUMB`, `SEQ`

After editing a CSV, reload the database from the project root:

```
python init_db.py
```
